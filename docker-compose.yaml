services:
  app:
    build: .
    container_name: urlbackend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    working_dir: /src
    volumes:
      - ./:/src
networks:
    default:
      name: urlnet